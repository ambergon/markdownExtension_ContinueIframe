from setuptools import setup, find_packages

setup(
    name='markdownExtension_ContinueIframe',
    version="0.0.1",
    description="iframeタグが自動で改行されないようにする。",
    author='ambergon',
    packages=find_packages()
)
